[project]
name = "local-agent-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.1",
    "ollama>=0.6.2",
    "pydantic>=2,<3",
    "python-dotenv>=1.2.2",
]
